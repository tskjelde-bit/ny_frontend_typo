import district from './district';
import blogPost from './blogPost';
import cityAverages from './cityAverages';
import siteConfig from './siteConfig';

export const schemaTypes = [district, blogPost, cityAverages, siteConfig];
